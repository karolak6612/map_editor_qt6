#include "Item.h"

// Constructor and destructor are inline in the header for this minimal placeholder.
// If more complex logic is added, they can be moved here.
// Item::Item(quint16 id, QObject* parent) : QObject(parent), id_(id) {}
// Item::~Item() {}
